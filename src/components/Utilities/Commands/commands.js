const commands = [
    {
        name: 'help',
        output : `These are the available commands for this interface:
        help - Displays all commands available
        pwd - Prints the present directory
        ls - Lists the files in the present directory
        `
    }
];

export default commands;